diff -Naur old_extract/mesa-25.0.7/include/drm-uapi/drm.h new_workspace/mesa-25.0.7/include/drm-uapi/drm.h
--- a/include/drm-uapi/drm.h	2025-05-28 10:20:23.000000000 -0500
+++ b/include/drm-uapi/drm.h	2026-06-20 10:33:08.840861621 -0500
@@ -44,7 +44,13 @@
 #else /* One of the BSDs */
 
 #include <stdint.h>
+
+#if !defined(__ETHEREAL__)
 #include <sys/ioccom.h>
+#else
+#include <asm/ioctls.h>
+#endif
+
 #include <sys/types.h>
 typedef int8_t   __s8;
 typedef uint8_t  __u8;
diff -Naur old_extract/mesa-25.0.7/meson.build new_workspace/mesa-25.0.7/meson.build
--- a/meson.build	2025-05-28 10:20:23.000000000 -0500
+++ b/meson.build	2026-06-20 09:58:02.410008825 -0500
@@ -127,7 +127,7 @@
 # Only build shared_glapi if at least one OpenGL API is enabled
 with_shared_glapi = with_shared_glapi and with_any_opengl
 
-system_has_kms_drm = ['openbsd', 'netbsd', 'freebsd', 'gnu/kfreebsd', 'dragonfly', 'linux', 'sunos', 'android', 'managarm'].contains(host_machine.system())
+system_has_kms_drm = ['openbsd', 'netbsd', 'freebsd', 'gnu/kfreebsd', 'dragonfly', 'linux', 'sunos', 'android', 'managarm', 'ethereal'].contains(host_machine.system())
 
 gallium_drivers = get_option('gallium-drivers')
 if gallium_drivers.contains('auto')
@@ -1036,7 +1036,7 @@
 endif
 
 # TODO: this is very incomplete
-if ['linux', 'cygwin', 'gnu', 'freebsd', 'gnu/kfreebsd', 'haiku', 'android', 'managarm'].contains(host_machine.system())
+if ['linux', 'cygwin', 'gnu', 'freebsd', 'gnu/kfreebsd', 'haiku', 'android', 'managarm', 'ethereal'].contains(host_machine.system())
   pre_args += '-D_GNU_SOURCE'
 elif host_machine.system() == 'sunos'
   pre_args += '-D__EXTENSIONS__'
diff -Naur old_extract/mesa-25.0.7/src/gallium/auxiliary/pipe-loader/pipe_loader_drm.c new_workspace/mesa-25.0.7/src/gallium/auxiliary/pipe-loader/pipe_loader_drm.c
--- a/src/gallium/auxiliary/pipe-loader/pipe_loader_drm.c	2025-05-28 10:20:23.000000000 -0500
+++ b/src/gallium/auxiliary/pipe-loader/pipe_loader_drm.c	2026-06-20 10:33:25.382444342 -0500
@@ -128,6 +128,9 @@
 static int
 get_nctx_caps(int fd, struct virgl_renderer_capset_drm *caps)
 {
+
+   DRM_IOWR(0xb4, struct drm_virtgpu_get_caps);
+
    struct drm_virtgpu_get_caps args = {
          .cap_set_id = VIRGL_RENDERER_CAPSET_DRM,
          .cap_set_ver = 0,
diff -Naur old_extract/mesa-25.0.7/src/gallium/auxiliary/renderonly/renderonly.c new_workspace/mesa-25.0.7/src/gallium/auxiliary/renderonly/renderonly.c
--- a/src/gallium/auxiliary/renderonly/renderonly.c	2025-05-28 10:20:23.000000000 -0500
+++ b/src/gallium/auxiliary/renderonly/renderonly.c	2026-06-20 10:24:28.247315667 -0500
@@ -37,6 +37,7 @@
 #include "util/u_inlines.h"
 #include "util/u_memory.h"
 
+
 void
 renderonly_scanout_destroy(struct renderonly_scanout *scanout,
 			   struct renderonly *ro)
diff -Naur old_extract/mesa-25.0.7/src/gallium/drivers/llvmpipe/lp_fence.c new_workspace/mesa-25.0.7/src/gallium/drivers/llvmpipe/lp_fence.c
--- a/src/gallium/drivers/llvmpipe/lp_fence.c	2025-05-28 10:20:23.000000000 -0500
+++ b/src/gallium/drivers/llvmpipe/lp_fence.c	2026-06-20 10:34:59.095151488 -0500
@@ -62,7 +62,14 @@
 
    do {
       clock_gettime(CLOCK_MONOTONIC, &poll_start);
+
+   #ifdef __ETHEREAL__
+      int timeout_ms = timeout / 1000000;
+      ret = poll(&fds, 1, timeout_ms);
+   #else
       ret = ppoll(&fds, 1, &timeout_ts, NULL);
+   #endif
+
       clock_gettime(CLOCK_MONOTONIC, &poll_end);
       if (ret > 0) {
          if (fds.revents & (POLLERR | POLLNVAL)) {
diff -Naur old_extract/mesa-25.0.7/src/gallium/targets/dril/dril_target.c new_workspace/mesa-25.0.7/src/gallium/targets/dril/dril_target.c
--- a/src/gallium/targets/dril/dril_target.c	2025-05-28 10:20:23.000000000 -0500
+++ b/src/gallium/targets/dril/dril_target.c	2026-06-20 10:03:16.816841796 -0500
@@ -350,8 +350,10 @@
 
    /* dlopen/dlsym to avoid linkage */
    egl = dlopen("libEGL.so.1", RTLD_LAZY | RTLD_LOCAL);
-   if (!egl)
+   if (!egl) {
+      fprintf(stderr, "error: missing libEGL.so.1\n");
       return false;
+   }
 
    void * (*peglGetProcAddress)(const char *) = dlsym(egl, "eglGetProcAddress");
    EGLDisplay (*peglGetPlatformDisplayEXT)(EGLenum, void *, const EGLint *) = peglGetProcAddress("eglGetPlatformDisplayEXT");
@@ -361,6 +363,8 @@
    EGLBoolean (*peglGetConfigAttrib)(EGLDisplay, EGLConfig, EGLint, EGLint *) = peglGetProcAddress("eglGetConfigAttrib");
    const char *(*peglQueryString)(EGLDisplay, EGLint) = peglGetProcAddress("eglQueryString");
 
+   fprintf(stderr, "dril: EGL client extensions: %s\n", peglQueryString(EGL_NO_DISPLAY, EGL_EXTENSIONS));
+
    struct gbm_device *gbm = NULL;
    if (fd != -1) {
       /* try opening GBM for hardware driver info */
@@ -369,20 +373,33 @@
          goto out;
    }
 
+#ifdef __ETHEREAL__
+   EGLDisplay dpy = peglGetPlatformDisplayEXT(EGL_PLATFORM_SURFACELESS_MESA, gbm ? gbm : EGL_DEFAULT_DISPLAY, NULL);
+#else
    EGLDisplay dpy = peglGetPlatformDisplayEXT(EGL_PLATFORM_GBM_MESA, gbm ? gbm : EGL_DEFAULT_DISPLAY, NULL);
-   if (!dpy)
+#endif
+
+   if (!dpy) {
+      fprintf(stderr, "error: bad peglGetPlatformDisplayEXT\n");
       goto out_gbm;
+   }
+
    int maj, min;
-   if (!peglInitialize(dpy, &maj, &min))
+   if (!peglInitialize(dpy, &maj, &min)) {
+      fprintf(stderr, "peglInitialize failed!\n");
       goto out_gbm;
+   }
 
    const char *egl_extension_list = peglQueryString(dpy, EGL_EXTENSIONS);
    bool has_srgb = strstr(egl_extension_list, "EGL_KHR_gl_colorspace");
 
    int num_configs = 0;
    EGLConfig *eglconfigs = NULL;
-   if (!peglGetConfigs(dpy, NULL, 0, &num_configs))
-      goto out_egl;
+   if (!peglGetConfigs(dpy, NULL, 0, &num_configs)) {
+    fprintf(stderr, "peglGetConfigs failed!\n");
+    goto out_egl;
+   }
+
    eglconfigs = malloc(sizeof(EGLConfig) * num_configs);
    /* overestimate: num_configs * doubleBuffer * sRGB + NULL */
    configs = calloc(num_configs * 2 * 2 + 1, sizeof(struct gl_config));
@@ -399,6 +416,7 @@
           !peglGetConfigAttrib(dpy, eglconfigs[i], EGL_SAMPLES, &samples))
          continue;
 
+      fprintf(stderr, "dril: config %u format=0x%x depth=%d stencil=%d samles=%d\n", i, format, depth, stencil, samples);
       enum pipe_format pformat = fourcc_to_pipe_format(format);
 
       /* srgb configs go after base configs */
@@ -430,6 +448,21 @@
    /* check last format for srgbness too */
    if (c && has_srgb)
       c = add_srgb_config(configs, c, last_pformat, last_start);
+
+   fprintf(stderr, "c: %u\n", c);
+   /* surfaceless EGL returns no native visual IDs, populate directly */
+if (c == 0) {
+   configs = calloc(ARRAY_SIZE(drilConfigs) * 2 + 1, sizeof(struct gl_config));
+   for (unsigned i = 0; i < ARRAY_SIZE(drilConfigs); i++) {
+      /* single-buffered */
+      configs[c] = mem_dup(&drilConfigs[i], sizeof(drilConfigs[i]));
+      configs[c++]->doubleBufferMode = 0;
+      /* double-buffered */
+      configs[c] = mem_dup(&drilConfigs[i], sizeof(drilConfigs[i]));
+      configs[c++]->doubleBufferMode = 1;
+   }
+}
+
 out_egl:
    free(eglconfigs);
    /* don't forget cleanup */
diff -Naur old_extract/mesa-25.0.7/src/glx/glxcmds.c new_workspace/mesa-25.0.7/src/glx/glxcmds.c
--- a/src/glx/glxcmds.c	2025-05-28 10:20:23.000000000 -0500
+++ b/src/glx/glxcmds.c	2026-06-20 10:02:34.218628001 -0500
@@ -1831,6 +1831,10 @@
 __glxGetMscRate(struct glx_screen *psc,
       int32_t * numerator, int32_t * denominator)
 {
+#ifdef __ETHEREAL__
+   return false; /* ethereal does not support __glxGetMscRate */
+#endif
+
 #if !defined(GLX_USE_WINDOWSGL)
    XF86VidModeModeLine mode_line;
    int dot_clock;
diff -Naur old_extract/mesa-25.0.7/src/util/detect_os.h new_workspace/mesa-25.0.7/src/util/detect_os.h
--- a/src/util/detect_os.h	2025-05-28 10:20:23.000000000 -0500
+++ b/src/util/detect_os.h	2026-06-20 09:56:02.103884960 -0500
@@ -91,6 +91,10 @@
 #define DETECT_OS_POSIX 1
 #endif
 
+#if defined(__ETHEREAL__)
+#define DETECT_OS_ETHEREAL 1
+#define DETECT_OS_POSIX 1
+#endif
 
 /*
  * Make sure DETECT_OS_* are always defined, so that they can be used with #if
diff -Naur old_extract/mesa-25.0.7/src/util/os_misc.c new_workspace/mesa-25.0.7/src/util/os_misc.c
--- a/src/util/os_misc.c	2025-05-28 10:20:23.000000000 -0500
+++ b/src/util/os_misc.c	2026-06-20 09:56:16.979472401 -0500
@@ -57,7 +57,7 @@
 #  include <unistd.h>
 #  include <log/log.h>
 #  include <cutils/properties.h>
-#elif DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD || DETECT_OS_MANAGARM
+#elif DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD || DETECT_OS_MANAGARM || DETECT_OS_ETHEREAL
 #  include <unistd.h>
 #elif DETECT_OS_OPENBSD || DETECT_OS_FREEBSD
 #  include <sys/resource.h>
@@ -284,7 +284,7 @@
 bool
 os_get_total_physical_memory(uint64_t *size)
 {
-#if DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD || DETECT_OS_MANAGARM
+#if DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || DETECT_OS_HURD || DETECT_OS_MANAGARM || DETECT_OS_ETHEREAL
    const long phys_pages = sysconf(_SC_PHYS_PAGES);
    const long page_size = sysconf(_SC_PAGE_SIZE);
 
diff -Naur old_extract/mesa-25.0.7/src/util/u_thread.c new_workspace/mesa-25.0.7/src/util/u_thread.c
--- a/src/util/u_thread.c	2025-05-28 10:20:23.000000000 -0500
+++ b/src/util/u_thread.c	2026-06-20 09:56:59.492594564 -0500
@@ -75,7 +75,7 @@
 void u_thread_setname( const char *name )
 {
 #if defined(HAVE_PTHREAD)
-#if DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || defined(__GLIBC__) || DETECT_OS_MANAGARM || DETECT_OS_FUCHSIA
+#if DETECT_OS_LINUX || DETECT_OS_CYGWIN || DETECT_OS_SOLARIS || defined(__GLIBC__) || DETECT_OS_MANAGARM || DETECT_OS_FUCHSIA || DETECT_OS_ETHEREAL
    int ret = pthread_setname_np(pthread_self(), name);
    if (ret == ERANGE) {
       char buf[16];
